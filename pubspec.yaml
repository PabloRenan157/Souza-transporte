name: s_transporte
description: Souza Transportes - Logística & Distribuição
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  provider: ^6.1.2
  supabase_flutter: ^2.6.0
  geolocator: ^13.0.1
  google_maps_flutter: ^2.9.0
  http: ^1.2.2
  intl: ^0.20.2
  shared_preferences: ^2.2.3
  excel: ^4.0.6
  csv: ^6.0.0
  share_plus: ^10.1.0
  uuid: ^4.4.0
  path_provider: ^2.1.3
  mobile_scanner: ^5.1.1
  flutter_local_notifications: ^17.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true